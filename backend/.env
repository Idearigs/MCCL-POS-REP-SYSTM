# Database Configuration
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=pos_mccl_db
DB_PORT=3306

# Google Drive API Configuration (leave as is for now if not set up)
GOOGLE_DRIVE_ROOT_FOLDER_ID=
GOOGLE_APPLICATION_CREDENTIALS=./path/to/your/service-account-file.json

# JWT Secret Key (replace with a strong, random string)
JWT_SECRET=mccl_pos_repair_secret_key